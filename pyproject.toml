[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eyeai"
version = "0.1.0"
description = "AMD detection from fundus images with RETFound, LoRA, and ensemble inference"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
