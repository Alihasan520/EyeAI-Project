# Add approved references here

Supported formats:

- PDF
- Markdown
- Plain text
- DOCX

Recommended folders:

- `guidelines/`
- `articles/`
- `internal_protocols/`
- `model_documentation/`

Every document should have a known title, publisher, publication date, and usage permission. The generated RAG answer cites only metadata stored with retrieved chunks; it does not create references from memory.
