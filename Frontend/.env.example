# Optional default API URL. Users can override it from the in-app connection panel.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Keep true while developing the interface without a running backend.
VITE_ENABLE_PREVIEW_MODE=true
